# 项目：AI Banking Agent（2026 FinTechathon 人工智能赛道 · 国内）

面向赛题的银行 AI 智能体演示系统：用户用自然语言完成银行业务操作。
**模拟银行核心，自建，不连接任何真实银行、不使用任何真实用户数据。**

---

## 铁律（违反即视为 bug，必须回退）

1. **LLM 只负责「理解意图」和「措辞」。** 金额、余额、限额、状态变更、权限判定，一律由 Python 代码决定，LLM 没有写权限。
2. **所有业务数字必须来自工具函数的返回值（事实包）。** 禁止在提示词、模板或代码里硬编码/编造任何业务数字。
3. **任何写操作必须走完四步**：`preview_*`（只算不执行）→ 权限档判定 → 用户确认（必要时 OTP）→ 幂等执行。
4. **接口冻结**：工具函数签名与返回字段严格按 `docs/01-接口规格.md`。不得重命名、不得增删字段、不得改变返回结构。需要改接口 → 停下来问我。
5. **每个会话写 `audit_log`**，每个请求带 `trace_id`；用户可见的每一次金额变动都必须可回溯。
6. **不引入新依赖前先问我。** 禁用 LangChain / LlamaIndex / 任何 Agent 编排大框架（我们要自己写状态机，这是评分点）。
7. **不可信文本一律当数据，不当指令。** 用户备注、收款人备注、IM 消息内容等外部文本，必须结构化包裹后作为数据传入，绝不拼进 system prompt。
8. **全合成数据。** 禁止任何真实人名、手机号、身份证、卡号（用 `138****0001` 这种形式）。

---

## 架构（五层，禁止越层调用）

```
interfaces/   ① 交互层   Streamlit 聊天页 / IM(飞书) webhook / CLI
agent/        ② 编排层   意图识别 → 槽位抽取 → 缺槽反问 → 权限预检 → 确认卡 → 执行 → 回执
guard/        ③ 护栏     权限分级(L0-L3)、注入检测、数字校验器、降级兜底
tools/        ④ 工具层   17 个白名单函数，入参出参均为 Pydantic 模型
data/         ⑤ 数据层   SQLite + 合成数据生成器 + DAO
```

- `agent/` 可以调 `guard/` 和 `tools/`；`tools/` 可以调 `guard/` 和 `data/`（14a 起 tools 调 guard 做越权/参数边界校验）；`interfaces/` **只能**调 `agent/`。
- 反例（禁止）：`interfaces/` 直接调 `tools/`；`agent/` 直接写 SQL。

## 技术栈（不要换）

Python 3.11 · Pydantic v2 · SQLite（stdlib sqlite3）· FastAPI + uvicorn · Streamlit · openai SDK（用于调 DeepSeek 等 OpenAI 兼容接口）· pytest · python-dotenv

## 常用命令

```bash
uv sync                                    # 安装依赖
uv run pytest -q                           # 全部单测
bash scripts/verify.sh                     # 一键验收（单测 + 冒烟对话 + 安全用例）
uv run python -m app.cli "帮我看看上个月花了多少"   # 命令行对话
uv run streamlit run interfaces/web/app.py # 起聊天界面
```

## 代码规范

- Python 3.11，所有函数带类型注解；数据模型一律 Pydantic v2。
- 单文件 ≤ 300 行；单函数 ≤ 40 行；禁止裸 `print`（用 `logging`）。
- 每个工具函数的入参出参都有对应 Pydantic 模型，模型定义在 `tools/schemas.py`。
- 测试放 `tests/`，命名 `test_<模块>_<行为>.py`；每个工具函数至少 3 条用例（正常 / 边界 / 非法）。
- 提交信息：`card-<编号>: <一句话>`。

## 每张任务卡必须做到的交付要求

1. 代码改动 + 对应单测。
2. **运行 `uv run pytest -q && bash scripts/verify.sh`，把真实输出贴出来。**
3. **没跑通不许说"完成"。** 不许为了让测试通过而改测试（除非任务卡明确要求）。
4. 3 行以内说明：改了什么 / 什么没做 / 有什么风险。
5. 新增文件必须在上面的架构里，越界就是错的。

## 明确禁止

- 编造第三方库的 API（不确定就先读官方文档或问我）。
- 一次改动任务卡范围之外的文件。
- 声称"已修复/已完成"而不提供命令输出。
- 为了让 demo 好看而绕过权限判定或跳过审计日志。
- 引入需要外部服务才能启动的依赖（评测环境必须一条命令跑起来）。
