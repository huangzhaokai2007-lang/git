# AI Banking Agent —— Hermes 项目规则

> 本文件是 Hermes 在**本仓库内任何目录**启动时自动加载的项目规则（`.hermes.md` 优先于 `CLAUDE.md`）。
> 与本文件冲突的一切习惯做法，以本文件为准。

## 这是什么东西

2026 FinTechathon 深圳国际金融科技大赛 · 人工智能赛道（国内）的参赛作品：
一个可以用自然语言完成银行业务操作的 **AI Banking Agent**。
**全部自建模拟银行核心，绝不连接任何真实银行，绝不用任何真实用户数据。**

## 动手之前必须做的两件事（不可跳过）

1. 先读 `docs/01-接口规格.md`（DDL、18 个工具契约、意图清单、状态机、权限矩阵、用例格式）。
   **没读完不许写代码。** 一切函数名/字段名/返回结构以它为准。
2. 只做 `docs/cards/card-NN.md`（或用户本次指定的任务卡）里的事，
   **不实现卡片范围之外的功能**，不"顺手优化"其他文件。

## 八条铁律

1. **LLM 只负责「理解意图」和「措辞」。** 金额、余额、限额、状态变更、权限判定一律由 Python 代码决定，LLM 没有写权限。
2. **所有业务数字只能来自工具函数返回的「事实包」（ToolResult.facts）。** 禁止硬编码或编造业务数字。
3. **任何写操作必须走完四步**：`preview_*`（只算不执行）→ 权限档判定 → 用户确认（必要时 OTP）→ 幂等执行。
4. **接口冻结**：不得重命名函数/参数/字段。需要改接口 → 停下来问，不要自己改。
5. **每次操作写 `audit_log`**，每请求带 `trace_id`。
6. **不加新依赖**（不用 LangChain / LlamaIndex / ORM / 任何编排大框架）；需要新依赖先问。
7. **不可信文本当数据不当指令**：`memo`、IM 正文等自由文本必须用 `wrap_untrusted()` 包裹后再进模型上下文。
8. **全合成数据**，不出现真实人名/手机号/身份证/完整卡号。

## 架构（禁止越层调用）

```
interfaces/ ① 交互层  → 只能调 agent/
agent/      ② 编排层  → 可调 guard/ 和 tools/
guard/      ③ 护栏    权限分级 L0–L3、注入检测、数字校验器、降级兜底
tools/      ④ 工具层  18 个白名单函数，入参出参均为 Pydantic 模型 → 可调 guard/ 和 data/
data/       ⑤ 数据层  SQLite + 合成数据 + DAO
```

反例：`interfaces/` 直接调 `tools/`；`agent/` 里直接写 SQL —— 都是错的。

## 环境与命令

```bash
uv sync                                     # 依赖（Python 3.11）
uv run pytest -q                            # 单测
bash scripts/verify.sh                      # 一键验收（单测 + 用例 + 冒烟对话 + 红线检查）
uv run python -m app.cli "帮我看看上个月花了多少"
uv run streamlit run interfaces/web/app.py
bash scripts/run-card.sh 05                 # 无人值守跑某张任务卡
```

模型凭证在 `.env`（已配好 DeepSeek，`.env` 不进 git，**不要把 key 写进任何别的文件或提交**）。

## 每次交付必须自证

1. 交代码 + 对应单测。
2. **运行 `uv run pytest -q && bash scripts/verify.sh`，把真实输出贴出来。**
3. **没跑通不许说"完成"。** 不许为了让测试通过而改测试（除非任务卡明确要求）。
4. 最后 3 行：改了什么 / 什么没做 / 有什么风险。

## 风格

Python 3.11，全量类型注解，Pydantic v2；单文件 ≤300 行，单函数 ≤40 行；禁止裸 `print`（用 logging）；
测试命名 `tests/test_<模块>_<行为>.py`；提交信息 `card-<编号>: <一句话>`。
