新增一个业务场景 $ARGUMENTS（严格按已有模式，不许重构现有代码）。

步骤：
1. 复述该场景在 docs/01-接口规格.md 里对应的意图、工具函数、权限档
2. 若规格中没有定义 → 停下来，先给我一份"需要新增的接口"清单，等我确认
3. 若已定义 → 按同样模式补齐：分类器槽位 / 编排分支 / 确认卡 / 模板 / YAML 用例
4. 至少补 3 条 YAML 用例（正常 / 边界 / 恶意）
5. 跑 `bash scripts/verify.sh` 并贴真实输出；不许改动已有场景的断言
